🧠 Бот Анна — Personal AI Secretary & Second Brain

Анна — это высокоэффективный секретарский интерфейс, объединяющий возможности Google Gemini 2.5, Telegram и твоего Obsidian. Она спроектирована для фильтрации хаоса, анализа тяжелого контента и управления задачами.

🔗 Репозиторий проекта

GitHub: https://github.com/your-username/sych-bot_poi

(Замени your-username на свой реальный логин)

🚀 Ключевые Компетенции Анны (v2.0)

1. YouTube Vision Native (Зрительная память)

Прямой анализ: Анна «смотрит» видео через Gemini 2.0/2.5 API без скачивания файлов на сервер. Ссылки передаются как fileUri, что гарантирует скорость и обход блокировок (403 Forbidden).

Глубокий конспект: Извлекает тезисы, алгоритмы и код в техническую заметку на русском языке.

AI-Именование: Нейросеть сама придумывает название файла на основе содержания (например, Как_работает_Docker.md).

2. Smart URL Extractor (Внимательность)

Находит ссылки везде:

В скрытых гиперссылках (text_link) внутри пересланных сообщений.

В подписях к медиа-файлам (caption).

В кнопках и обычном тексте.

3. Веб-клиппер & Переводчик

Любая статья на иностранном языке (Medium, Dev.to и др.) автоматически переводится и структурируется на русском языке перед сохранением.

Очистка от рекламы (Readability + Turndown).

4. Менеджер задач (Task Logger)

Ведет лог твоих дел в файле ANNA_TASKS.md прямо в Obsidian по команде «Анна, запиши...» или «Напомни...».

5. Система Приоритетов (Logic Pipeline)

Бот обрабатывает входящие сообщения по строгой иерархии:

Ручное сохранение (Reply): Ответ на сообщение с текстом мд / save — принудительно сохраняет контент (видео, текст, статью).

Авто-сохранение (Forward): Пересланные сообщения из каналов сохраняются автоматически без лишних вопросов.

Клиппер: Сообщения, содержащие только ссылку.

AI Чат: Всё остальное воспринимается как диалог с секретарем.

🏗 Архитектурный стандарт

Проект построен по модульному принципу "Blocks & Sub-blocks" для чистоты кода и легкой поддержки:

src/core/logic.js — Центральный процессор. Маршрутизирует сообщения по приоритетам.

src/services/video_vision.js — Модуль нативного анализа видео (таймаут 10 минут, санитизация имен).

src/services/parser.js — Сборщик знаний. Парсер статей с AI-переводом и защитой от бот-фильтров (User-Agent).

src/services/ai.js — Интеллектуальное ядро. Управление моделями Gemini, ротация ключей и проброс ошибок.

⚙️ Развертывание (Linux / Docker)

Путь на сервере: /root/sych-bot_poi/

# 1. Очистка от старых контейнеров (если нужно)
docker system prune -f

# 2. Сборка и запуск
docker compose up -d --build

# 3. Проверка логов
docker compose logs -f sych-bot


Переменные окружения (.env)

Файл .env должен быть создан в корне проекта (он игнорируется git):

TELEGRAM_BOT_TOKEN=ваш_токен
ADMIN_USER_ID=ваш_id
OPENROUTER_API_KEY=ваш_ключ_openrouter (для Gemini)
GOOGLE_API_KEY=ваш_ключ_google_aistudio (для Vision)
AI_MODEL=google/gemini-2.5-flash-lite
OBSIDIAN_PATH=/app/obsidian_inbox


📋 Протоколы взаимодействия

ПОП (Протокол Проверки) — Перед внедрением решения проводится проверка по 3+ независимым источникам.

КМП (Как Меня Поняла) — Команда для получения краткой выжимки интерпретации задачи.

Языковой фильтр — Весь исходящий контент строго на русском языке.

Секретарь Анна: Ваша личная OS для системного мышления.